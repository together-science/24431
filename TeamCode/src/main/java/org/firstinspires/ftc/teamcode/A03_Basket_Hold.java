package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.Autonomous;

@Autonomous(name="A04 Basket Hold", group="Robot")

public class A03_Basket_Hold extends BaseAuto {

    protected void auto() {
        // strafe(DRIVE_SPEED_SLOW,14.0, 90.0);
        // driveStraight(DRIVE_SPEED_SLOW, -60.0);    // Drive Forward 3
        // moveArmTo(ARM_BASKET); 
        // turnToHeading(TURN_SPEED, 45.0); 
        // driveStraight(DRIVE_SPEED_SLOW, -22.0);
        // openGrabber();
        // sleep(1000);
        // closeGrabber();
        // moveArmTo(ARM_INIT);
        // driveStraight(DRIVE_SPEED_SLOW, 20.0); 
        // turnToHeading(TURN_SPEED, 0); 
        // driveStraight(DRIVE_SPEED_NORMAL, 100.0); 
        // turnToHeading(TURN_SPEED, -90.0); 
        // driveStraight(DRIVE_SPEED_SLOW, 17.0); 
          
    }
}
