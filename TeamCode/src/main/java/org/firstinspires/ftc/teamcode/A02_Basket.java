package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.Autonomous;

@Autonomous(name="A03 Basket", group="Robot")

public class A02_Basket extends BaseAuto {

    protected void auto() {
        // strafe(DRIVE_SPEED_SLOW,7.0, 90.0);
        // driveStraight(DRIVE_SPEED_SLOW, -20.0);    // Drive Forward 3
        // moveArmTo(ARM_BASKET); 
        // turnToHeading(TURN_SPEED, 45.0); 
        // driveStraight(DRIVE_SPEED_SLOW, -8.0);
        // openGrabber();
        // sleep(1000);
        // closeGrabber();
        // moveArmTo(ARM_INIT); 
        // driveStraight(DRIVE_SPEED_SLOW, 20.0); 
        // turnToHeading(TURN_SPEED, 0); 
        // driveStraight(DRIVE_SPEED_NORMAL, 90.0); 
        // turnToHeading(TURN_SPEED, -90.0); 
        // driveStraight(DRIVE_SPEED_SLOW, 15.0); 
          
    }
}
